# Valores usados no BUILD de produção (npm run build). Commitado de propósito:
# garante um build correto mesmo sem o .env local. NÃO contém segredos.
#
# A senha do painel NÃO fica aqui — o login é validado no backend.
VITE_USE_MOCK=false
VITE_API_URL=/api
